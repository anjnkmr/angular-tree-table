import { TreeTableRowAction } from './tree-table-row-action';

describe('TreeTableRowAction', () => {
  it('should create an instance', () => {
    expect(new TreeTableRowAction()).toBeTruthy();
  });
});
