import { TreeTableRowActionType } from './tree-table-row-action-type';

describe('TreeTableRowActionType', () => {
  it('should create an instance', () => {
    expect(new TreeTableRowActionType()).toBeTruthy();
  });
});
