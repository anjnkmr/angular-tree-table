import { TreeTableHeaderObject } from './tree-table-header-object';

describe('TreeTableHeaderObject', () => {
  it('should create an instance', () => {
    expect(new TreeTableHeaderObject()).toBeTruthy();
  });
});
